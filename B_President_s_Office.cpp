#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m; cin >> n >> m;
    char c; cin >> c;
    bool start = true;
    int x1,y1,x2,y2;
    vector<string> arr(n);
    for (int i=0;i<n;i++) {
        cin >> arr[i];
    }
    set<char> uni;


}