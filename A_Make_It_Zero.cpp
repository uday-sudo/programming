#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, temp;
        cin >> n;
        vector<int> arr;
        for (int i=1;i<=n;i++) {
            cin >> temp;
            arr.push_back(temp);
        }
    }
}